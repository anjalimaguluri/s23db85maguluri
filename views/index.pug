//- extends layout

//- block content
//-   h1= title
//-   p Welcome to #{title}

//-   p Naganjali Maguluri
  
//-   button(type="button",onclick="doReduce()" class="btn btn-primary") Press Me

//-   div(id="reduce") Reduce the value
//-   script.
//-     var count=20;
//-     var change=-1;
//-     function doReduce() {
//-       count = count+change;
//-       change--;
//-       document.getElementById("reduce").innerHTML=`Count ${count} with change ${change}`
//-     }

extends layout
block content
    if (!user)
      a(href="/login") Login
      br
      a(href="/register") Register
    if (user)
      p You are currently logged in as #{user.username}
      a(href="/employee") Take me to employee
      br
      a(href="/logout") Logout